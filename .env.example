# Copy to .env and fill real values. .env is gitignored — NEVER commit real secrets.

# --- PostgreSQL / Supabase (bắt buộc khi chạy backend/docker-compose) ---
# Backend đọc ConnectionStrings__DefaultConnection; docker-compose đọc DB_CONNECTION_STRING.
ConnectionStrings__DefaultConnection=Host=db.hlkzysbaebmxaogrexmv.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=CHANGE_ME;SSL Mode=Require;Trust Server Certificate=true
DB_CONNECTION_STRING=Host=db.hlkzysbaebmxaogrexmv.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=CHANGE_ME;SSL Mode=Require;Trust Server Certificate=true

# --- JWT (bắt buộc ngoài Development; >= 32 ký tự) ---
Jwt__Key=CHANGE_ME_to_a_long_random_secret_at_least_32_chars

# --- Cloudinary (bắt buộc khi dùng upload ảnh/video) ---
Cloudinary__CloudName=de0npovxi
Cloudinary__ApiKey=796116736931122
Cloudinary__ApiSecret=CHANGE_ME

# --- Gemini AI Assistant (tuỳ chọn) ---
Gemini__ApiKey=
